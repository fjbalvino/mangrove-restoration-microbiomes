{
  "script": "06_gene_associations/06_03_environment_mixed_models.R",
  "original_source": "/home/fjbalvino/Tipping_points/scripts_finales/603c_asociaciones_bloques_por_profundidad.R",
  "original_sha256": "8800c1c578c6a5b0f301c88f38a97d4e6a5d7246cbb895f29a8833203f59f0a5",
  "purpose_and_status": "Five separately fitted axes; BH pooled across axes within common, interaction and depth-slope families.",
  "inputs_static_expressions": [
    "read_tsv <- function(p) read.delim(p, check.names = FALSE)",
    "if (!nzchar(source_run)) source_run <- trimws(readLines(pointer, n = 1L))",
    "s <- read_tsv(paths[1])",
    "m <- read.csv(paths[2], check.names = FALSE)",
    "selection <- read_tsv(paths[4])",
    "z <- tryCatch(read.delim(con, check.names = FALSE), finally = close(con))"
  ],
  "outputs_static_expressions": [
    "write_tsv <- function(x, p) data.table::fwrite(x, p, sep = \"\\t\", na = \"NA\")",
    "writeLines(capture.output(sessionInfo()), file.path(run, \"logs\", \"603c_sessionInfo.txt\"))",
    "write_tsv(data.frame(input = paths, md5 = unname(hashes)), file.path(run, \"tables\", \"603c_inputs.tsv\"))",
    "write_tsv(m[, setdiff(names(m), \"axis_z\")], file.path(run, \"tables\", \"603c_metadata_used.tsv\"))",
    "write_tsv(scaling, file.path(run, \"tables\", \"603c_axis_audit.tsv\"))",
    "write_tsv(data.frame(axis = rownames(correlations), correlations, check.names = FALSE),",
    "writeLines(c(",
    "saveRDS(part, file.path(run, \"checkpoints\", sprintf(\"axis%02d_batch%04d.rds\", a, h)))",
    "write_tsv(r, file.path(run, \"tables\", \"603c_all_results.tsv.gz\"))",
    "write_tsv(diagnostics, file.path(run, \"tables\", \"603c_fit_diagnostics.tsv\"))",
    "write_tsv(families, file.path(run, \"tables\", \"603c_test_families.tsv\"))",
    "write_tsv(by_axis, file.path(run, \"tables\", \"603c_summary_by_axis.tsv\"))",
    "write_tsv(data.frame(status = status, mode = mode, samples = 51, profiles = 17,",
    "writeLines(run, tmp)"
  ],
  "path_literals": [
    "/home/fjbalvino/Tipping_points/resultados_finales",
    "LATEST_601b_preparar_CLR_proteico_por_muestra.txt",
    "601b_run_summary.tsv",
    "601b_metadata_51samples.csv",
    "601b_top_100000_CLR_features_x_samples.tsv.gz",
    "601b_selected_features_top_100000.tsv",
    "CLR no numerico/no finito",
    "603c_sessionInfo.txt",
    ".R",
    "603c_inputs.tsv",
    "603c_metadata_used.tsv",
    "603c_axis_audit.tsv",
    "603c_predictor_correlations.tsv",
    "603c_protocol.txt",
    "/",
    "axis%02d_batch%04d.rds",
    "603c_all_results.tsv.gz",
    "603c_fit_diagnostics.tsv",
    "603c_test_families.tsv",
    "603c_summary_by_axis.tsv",
    "Ninguna prueba utilizable en al menos un eje/familia; revisar",
    "603c_run_summary.tsv",
    ".txt"
  ],
  "packages_static": [
    "data.table",
    "emmeans",
    "lme4",
    "lmerTest",
    "p",
    "parallel",
    "tools"
  ],
  "resolution": "static expressions; runtime arguments and branches not asserted to be resolved",
  "execution_here": "not rerun"
}
